## Deployment

### Production build

To generate a production build of the plugin, run the following bash script inside of the project.

```bash
bash bin/compile-plugin.sh
```

This will produce a production ready build of the plugin ready to be installed on a WordPress site of the latest develop branch.